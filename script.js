// script.js\ndocument.addEventListener("DOMContentLoaded", () => {\n    const tabs = document.querySelectorAll(".tab");\n    const postsContainer = document.getElementById("posts-container");\n    let posts = [];\n\n    // Load posts from posts.json\n    async function loadPosts() {\n        const response = await fetch("posts.json");\n        posts = await response.json();\n        displayPosts("home"); // default to home tab\n    }\n\n    // Tab switching functionality\n    tabs.forEach(tab => {\n        tab.addEventListener("click", () => {\n            const category = tab.dataset.category;\n            displayPosts(category);\n        });\n    });\n\n    // Display posts based on category\n    function displayPosts(category) {\n        postsContainer.innerHTML = ""; // clear old posts\n        let filteredPosts = category === "home" ? shuffleArray(posts) : posts.filter(post => post.category === category);\n        \n        filteredPosts.forEach(post => {\n            const postElement = createPostElement(post);\n            postsContainer.appendChild(postElement);\n        });\n    }\n\n    // Shuffle posts array\n    function shuffleArray(array) {\n        for (let i = array.length - 1; i > 0; i--) {\n            const j = Math.floor(Math.random() * (i + 1));\n            [array[i], array[j]] = [array[j], array[i]];\n        }\n        return array;\n    }\n\n    // Create post element based on category\n    function createPostElement(post) {\n        const div = document.createElement("div");\n        div.className = `post ${post.category}`;\n        div.innerHTML = `\n            <h2>${post.title}</h2>\n            <p>${post.content}</p>\n            <button class=\"open-modal\" data-id=\"${post.id}\">View More</button>\n        `;\n        div.querySelector(".open-modal").addEventListener("click", () => openModal(post));\n\n        return div;\n    }\n\n    // Open modal for expanded view\n    function openModal(post) {\n        const modal = document.getElementById("modal");\n        modal.innerHTML = `\n            <div class=\"modal-content\">\n                <span class=\"close-modal\">&times;</span>\n                <h2>${post.title}</h2>\n                <p>${post.content}</p>\n            </div>\n        `;\n        modal.style.display = "block";\n\n        modal.querySelector(".close-modal").onclick = () => {\n            modal.style.display = "none";\n        };\n    }\n\n    loadPosts(); // Initial load of posts\n});\n